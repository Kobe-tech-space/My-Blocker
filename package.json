{
  "name": "static-blog",
  "version": "1.0.0",
  "description": "A lightweight static blog generator",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0"
  }
}
